# Comando inicial para docker

Este comando para configurar postgres. Cambiar la ruta(tiene que ser absoluta creo).

```docker
docker run --name postgres -p 5432:5432 -v D:/AAA/orms/z_postgres/pg:/var/lib/postgresql/data -e POSTGRES_PASSWORD=pepenacho  postgres
```
